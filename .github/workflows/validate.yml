name: Validate

on:
  push:
  pull_request:

jobs:
  m0:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Shell syntax
        run: bash -n build.sh
      - name: Board metadata preflight
        run: bash build.sh orangepi-zero-lts --check-only
